import{render,screen,waitFor}from'@testing-library/react';import userEvent from'@testing-library/user-event';import{afterEach,describe,expect,it,vi}from'vitest';import{LessonPage}from'./App';
const lesson={id:'pandas-intro',title:'Работа с данными в Python. Первое знакомство с pandas',age_group:'7–8 классы',content_version:'1.0.0',blocks:[{id:'block',title:'Датасет, CSV и DataFrame',order:1,theory:{parts:[{type:'heading',text:'Данные как таблица'},{type:'paragraph',text:'Датасет — это набор данных.'}]},exercises:[1,2,3].map((n)=>({id:`e${n}`,title:`Упражнение ${n}`,difficulty:n===1?'basic':n===2?'intermediate':'advanced',statement:`Условие ${n}`,starter_code:'import pandas as pd',hints:['Подсказка'],public_examples:[],assets:[],order:n}))}]};const progress={lesson_id:'pandas-intro',completed:0,total:3,percent:0,completed_exercise_ids:[]};
function mockOk(){vi.stubGlobal('fetch',vi.fn().mockResolvedValueOnce({ok:true,status:200,json:async()=>lesson}).mockResolvedValueOnce({ok:true,status:200,json:async()=>progress}))}
afterEach(()=>vi.unstubAllGlobals());describe('lesson screen',()=>{it('shows loading, title, theory and progress semantics',async()=>{mockOk();render(<LessonPage lessonId="pandas-intro"/>);expect(screen.getByText(/Загружаем/)).toBeInTheDocument();expect(await screen.findByRole('heading',{name:/Работа с данными/})).toBeInTheDocument();expect(screen.getByText('Датасет — это набор данных.')).toBeInTheDocument();expect(screen.getByText('0 / 3 заданий')).toBeInTheDocument();expect(screen.getByRole('progressbar')).toHaveAttribute('value','0');expect(screen.getByRole('progressbar')).toHaveAttribute('max','100')});it('switches through all exercises and back to theory',async()=>{mockOk();const user=userEvent.setup();render(<LessonPage lessonId="pandas-intro"/>);await screen.findByText('Датасет — это набор данных.');for(let n=1;n<=3;n++){await user.click(screen.getByRole('button',{name:`Задача ${n}`}));expect(screen.getByRole('heading',{name:`Упражнение ${n}`})).toBeInTheDocument()}await user.click(screen.getByRole('button',{name:'Теория'}));expect(screen.getByText('Датасет — это набор данных.')).toBeInTheDocument()});it('shows API errors',async()=>{vi.stubGlobal('fetch',vi.fn().mockRejectedValue(new Error('API_ERROR')));render(<LessonPage lessonId="pandas-intro"/>);await waitFor(()=>expect(screen.getByRole('heading',{name:'Не удалось загрузить урок'})).toBeInTheDocument())})});
