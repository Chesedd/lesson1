import{render,screen,waitFor}from'@testing-library/react';import userEvent from'@testing-library/user-event';import{describe,expect,it,vi}from'vitest';import{LessonPage}from'./App';import type{LearningService}from'./services/learning';
const lesson={id:'pandas-intro',title:'Работа с данными в Python. Первое знакомство с pandas',age_group:'7–8 классы',content_version:'1.0.0',blocks:[{id:'block',title:'Датасет, CSV и DataFrame',order:1,theory:{parts:[{type:'heading' as const,text:'Данные как таблица'},{type:'paragraph' as const,text:'Датасет — это набор данных.'}]},exercises:[1,2,3].map((n)=>({id:`e${n}`,title:`Упражнение ${n}`,difficulty:(n===1?'basic':n===2?'intermediate':'advanced') as 'basic'|'intermediate'|'advanced',statement:`Условие ${n}`,starter_code:'import pandas as pd',hints:['Подсказка'],public_examples:[],assets:[],order:n}))}]};const progress={lesson_id:'pandas-intro',completed:0,total:3,percent:0,completed_exercise_ids:[]};
function service(overrides:Partial<LearningService>={}):LearningService{return{loadLesson:vi.fn().mockResolvedValue(lesson),loadLessonProgress:vi.fn().mockResolvedValue(progress),...overrides} as LearningService}
describe('lesson screen',()=>{it('shows loading, title, theory and progress semantics',async()=>{render(<LessonPage lessonId="pandas-intro" service={service()}/>);expect(screen.getByText(/Загружаем/)).toBeInTheDocument();expect(await screen.findByRole('heading',{name:/Работа с данными/})).toBeInTheDocument();expect(screen.getByText('Датасет — это набор данных.')).toBeInTheDocument();expect(screen.getByText('0 / 3 заданий')).toBeInTheDocument();expect(screen.getByText('0%')).toBeInTheDocument();expect(screen.getByRole('progressbar')).toHaveAttribute('value','0');expect(screen.getByRole('progressbar')).toHaveAttribute('max','100')});it('switches through all exercises and back to theory',async()=>{const user=userEvent.setup();render(<LessonPage lessonId="pandas-intro" service={service()}/>);await screen.findByText('Датасет — это набор данных.');for(let n=1;n<=3;n++){await user.click(screen.getByRole('button',{name:`Задача ${n}`}));expect(screen.getByRole('heading',{name:`Упражнение ${n}`})).toBeInTheDocument()}await user.click(screen.getByRole('button',{name:'Теория'}));expect(screen.getByText('Датасет — это набор данных.')).toBeInTheDocument()});it('shows application errors',async()=>{render(<LessonPage lessonId="pandas-intro" service={service({loadLesson:vi.fn().mockRejectedValue(new Error('APP_ERROR'))})}/>);await waitFor(()=>expect(screen.getByRole('heading',{name:'Не удалось загрузить урок'})).toBeInTheDocument())});it('shows unknown lesson errors',async()=>{render(<LessonPage lessonId="missing" service={service({loadLesson:vi.fn().mockRejectedValue(new Error('NOT_FOUND'))})}/>);expect(await screen.findByRole('heading',{name:'Урок не найден'})).toBeInTheDocument()})});
